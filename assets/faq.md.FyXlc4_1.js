import{_ as s,C as n,o as r,c as d,ae as a,j as t,a as o,E as l,w as c}from"./chunks/framework.D7zHIyZF.js";const w=JSON.parse('{"title":"FAQ","description":"","frontmatter":{"title":"FAQ"},"headers":[],"relativePath":"faq.md","filePath":"faq.md"}'),h={name:"faq.md"};function u(p,e,m,y,f,g){const i=n("nobr");return r(),d("div",null,[e[5]||(e[5]=a('<h1 id="faq" tabindex="-1">FAQ <a class="header-anchor" href="#faq" aria-label="Permalink to &quot;FAQ&quot;">​</a></h1><h2 id="what-is-jai" tabindex="-1">What is jai? <a class="header-anchor" href="#what-is-jai" aria-label="Permalink to &quot;What is jai?&quot;">​</a></h2><p>jai is a super-lightweight Linux sandbox for AI agents and coding assistants. It uses Linux mount namespaces and overlayfs to give a sandboxed process full access to your working directory, copy-on-write access to your home directory, private temp directories, and read-only access to the rest of the filesystem. In strict mode, it can also hide the contents of your home directory and provide access to the rest of the system with a different UID. The goal is to reduce the blast radius when running AI tools on your real machine — not to promise perfect isolation.</p><h2 id="was-jai-written-by-an-ai-coding-agent" tabindex="-1">Was jai written by an AI coding agent? <a class="header-anchor" href="#was-jai-written-by-an-ai-coding-agent" aria-label="Permalink to &quot;Was jai written by an AI coding agent?&quot;">​</a></h2>',4)),t("p",null,[e[1]||(e[1]=o("No. While this web site was obviously made by an LLM (ChatGPT read the man page, asked some follow-up questions, and produced a prompt from which claude code built a vitepress site), jai itself was hand implemented by a Stanford computer science professor with decades of C++ and Unix/linux experience. As an experiment, the author did previously try vibe-coding a container, but the results were disastrous and repeatedly put his machine in a state that required a reboot (e.g., recursively changing the attributes of all mounts in the wrong mount namespace). The author ",-1)),e[2]||(e[2]=t("em",null,"does",-1)),e[3]||(e[3]=o(" use coding agents to look for bugs, get feedback, and develop tests. However, rest assured that a single human understands every line of ",-1)),l(i,null,{default:c(()=>[...e[0]||(e[0]=[o("C++",-1)])]),_:1}),e[4]||(e[4]=o(" in jai.",-1))]),e[6]||(e[6]=a('<h2 id="which-mode-should-i-use" tabindex="-1">Which mode should I use? <a class="header-anchor" href="#which-mode-should-i-use" aria-label="Permalink to &quot;Which mode should I use?&quot;">​</a></h2><ul><li><strong>Casual</strong> (the default for unnamed jails) if you want the least friction and mostly care about preventing filesystem damage. Good for quick interactive work where you trust the tool enough to read your files but not to write everywhere.</li><li><strong>Strict</strong> (the default for named jails) if you want real confidentiality — the jailed process runs as a different user and cannot read your files. Best for isolating tools that should not see each other&#39;s credentials.</li><li><strong>Bare</strong> if you need strict-like home isolation but your home directory is on NFS, which does not support id-mapped mounts.</li></ul><p>See <a href="/modes.html">Modes</a> for a full comparison.</p><h2 id="why-is-casual-mode-not-enough-for-confidentiality" tabindex="-1">Why is casual mode not enough for confidentiality? <a class="header-anchor" href="#why-is-casual-mode-not-enough-for-confidentiality" aria-label="Permalink to &quot;Why is casual mode not enough for confidentiality?&quot;">​</a></h2><p>In casual mode, the jailed process runs as <strong>your user</strong>. It can read anything you can read — SSH keys, API tokens, browser session data, and every other file in your home directory. The copy-on-write overlay protects integrity (writes go to a separate layer), but it does not hide the underlying files.</p><p>The <code>--mask</code> option and default blacklists hide some known sensitive dotfiles, but casual mode exposes everything readable by default. You cannot exhaustively enumerate and mask every sensitive file. If confidentiality matters, use strict mode.</p><h2 id="what-should-i-do-if-my-home-directory-is-on-nfs" tabindex="-1">What should I do if my home directory is on NFS? <a class="header-anchor" href="#what-should-i-do-if-my-home-directory-is-on-nfs" aria-label="Permalink to &quot;What should I do if my home directory is on NFS?&quot;">​</a></h2><p>You should place your jai overlay storage on a local file system. You can do this with symbolic links, but it is easier to relocate the storage base directory with <code>--storage=/local/path</code> (or <code>storage /local/path</code> in <code>$HOME/.jai/.defaults</code>). You will also need to use bare mode instead of strict mode in most cases, so might want to add <code>mode bare</code> to your <code>$HOME/.jai/.defaults</code> file.</p><h2 id="why-does-casual-mode-require-a-non-nfs-storage-directory" tabindex="-1">Why does casual mode require a non-NFS storage directory? <a class="header-anchor" href="#why-does-casual-mode-require-a-non-nfs-storage-directory" aria-label="Permalink to &quot;Why does casual mode require a non-NFS storage directory?&quot;">​</a></h2><p>The linux overlay file system implementation makes use of extended file attributes (e.g., <code>trusted.overlay.origin</code>) to record aspects of the correspondence between an &quot;upper&quot; changes directory and the &quot;lower&quot; read-only home directory. Unfortunately, these attributes cannot be set or accessed over NFS. Also, jai needs root privileges to create whiteout files for masked paths, and often NFS clients do not have root access to remote file systems. Fortunately, the &quot;lower&quot; directory can be on NFS, so you can use casual mode with an NFS home directory as long as you put the upper &quot;<code>.changes</code>&quot; directory someplace local.</p><h2 id="why-does-strict-mode-not-work-with-nfs" tabindex="-1">Why does strict mode not work with NFS? <a class="header-anchor" href="#why-does-strict-mode-not-work-with-nfs" aria-label="Permalink to &quot;Why does strict mode not work with NFS?&quot;">​</a></h2><p>Strict mode uses <strong>id-mapped mounts</strong> to make granted directories appear owned by the unprivileged <code>jai</code> user inside the jail, even though the real files are owned by the invoking user. NFS does not support id-mapped mounts — this is a kernel/NFS implementation limitation, not a jai limitation. This is why bare mode exists.</p><p>Note that if your home directory is on NFS but you don&#39;t grant access to any part of your home directory (e.g., <code>jai -D</code>), and if you relocate your storage with <code>--storage=/local/path</code>, then strict mode will work. But generally people want to run <code>jai</code> to work in some subdirectory of their home directory.</p><h2 id="why-does-jai-not-support-linux-versions-earlier-than-6-13" tabindex="-1">Why does jai not support Linux versions earlier than 6.13? <a class="header-anchor" href="#why-does-jai-not-support-linux-versions-earlier-than-6-13" aria-label="Permalink to &quot;Why does jai not support Linux versions earlier than 6.13?&quot;">​</a></h2><p>Linux 6.13 added support for <code>fsconfig</code> <code>FSCONFIG_SET_FD</code> to overlayfs. That makes it much easier for privileged software like jai to avoid time-of-check-to-time-of-use (TOCTTOU) vulnerabilities when creating overlay mounts. Linux 6.12 was released in 2024, three months before claude code. You really shouldn&#39;t let your kernel lag so far behind AI agents that may try to attack it.</p><h2 id="where-do-changes-go-in-casual-mode" tabindex="-1">Where do changes go in casual mode? <a class="header-anchor" href="#where-do-changes-go-in-casual-mode" aria-label="Permalink to &quot;Where do changes go in casual mode?&quot;">​</a></h2><p>Writes to your home directory inside a casual jail go to:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>$HOME/.jai/default.changes/    # unnamed jails</span></span>\n<span class="line"><span>$HOME/.jai/&lt;name&gt;.changes/     # named jails</span></span></code></pre></div><p>These directories are the &quot;upper&quot; layer of the overlayfs mount. To move changes back to your real home directory, run <code>jai -u</code> to unmount the overlay, then copy the files.</p><p>If you specified <code>--storage=dir</code>, the changes directory is under <code>dir</code> instead.</p><h2 id="can-i-re-expose-a-file-in-the-overlay-home-after-deleting-or-masking-it" tabindex="-1">Can I re-expose a file in the overlay home after deleting or masking it? <a class="header-anchor" href="#can-i-re-expose-a-file-in-the-overlay-home-after-deleting-or-masking-it" aria-label="Permalink to &quot;Can I re-expose a file in the overlay home after deleting or masking it?&quot;">​</a></h2><p>Yes. The overlay file system uses whiteout files in the &quot;upper&quot; directory to record deletion. These are just character device nodes with major/minor number 0/0. To remove a whiteout file, first unmount the overlay file system with <code>jai -u</code>. Then change to the upper directory, generally in <code>~/.jai/default.changes</code> (or substitute the name of your jail for <code>default</code>). Find the whiteout file among the changed files there, and just delete it.</p><p>Note that if you delete a whiteout file without first tearing down the overlay with <code>jai -u</code>, you may not see the effects, as the overlay file system can cache information and doesn&#39;t expect concurrent modifications of the upper directory.</p><h2 id="can-i-access-my-overlay-home-directory-outside-of-jai" tabindex="-1">Can I access my overlay home directory outside of jai? <a class="header-anchor" href="#can-i-access-my-overlay-home-directory-outside-of-jai" aria-label="Permalink to &quot;Can I access my overlay home directory outside of jai?&quot;">​</a></h2><p>Yes, once jai has created an overlay home, you can find it (outside of a sandbox) under <code>/run/jai/$USER/default.home</code>. You can also find the private <code>/tmp</code> directories under <code>/run/jai/$USER/tmp/</code>. One use for this is to delete files containing sensitive data from the overlay (as an alternative to <code>--mask</code>) from outside a sandbox. Be extra, extra careful, however, because the overlay will look like your home directory, and you don&#39;t want to get into the habit if deleting important files in places that look like your home directory. One sanity check is to run <code>df .</code>. In an overlay directory, the file system will be named something like <code>jai-default.home</code> as opposed to a block device like <code>/dev/sda4</code>.</p><h2 id="what-does-jai-u-do" tabindex="-1">What does <code>jai -u</code> do? <a class="header-anchor" href="#what-does-jai-u-do" aria-label="Permalink to &quot;What does `jai -u` do?&quot;">​</a></h2><p><code>jai -u</code> tears down the sandbox state:</p><ul><li>Unmounts all overlay directories from <code>/run/jai/$USER</code></li><li>Cleans up the overlay work directories (<code>$HOME/.jai/*.work</code>), which sometimes contain root-owned files users cannot delete</li><li>Destroys private <code>/tmp</code> and <code>/var/tmp</code> directories</li></ul><p>Run <code>jai -u</code> when you want a fresh start, when you&#39;ve changed <code>--mask</code> options (masks are only applied at overlay creation time), or when overlays are behaving strangely.</p><h2 id="is-there-a-system-level-jai-u-that-applies-to-all-users" tabindex="-1">Is there a system-level <code>jai -u</code> that applies to all users? <a class="header-anchor" href="#is-there-a-system-level-jai-u-that-applies-to-all-users" aria-label="Permalink to &quot;Is there a system-level `jai -u` that applies to all users?&quot;">​</a></h2><p>As root, to destroy all mounts of all users, run <code>umount -R /run/jai</code>. You will see errors for sandboxes that are still in use, and can kill these processes to free the sandbox.</p><p>Note: avoid running <code>umount -Rl /run/jai</code>, which forcibly detaches mounts even if they are in use, as that does not actually free the mounts. Hence, if processes are still using overlay home directories, they will be unable to launch new sandboxes, because the old overlay will still exist, just not be accessible from the primary mount namespace.</p><h2 id="how-can-i-protect-my-local-git-repository" tabindex="-1">How can I protect my local git repository? <a class="header-anchor" href="#how-can-i-protect-my-local-git-repository" aria-label="Permalink to &quot;How can I protect my local git repository?&quot;">​</a></h2><p>By default, git stores all of its repository information below your project directory, in a subdirectory called <code>.git</code>. That means if jai gives an agent access to your project directory, the agent can wipe not just your project but its local git history.</p><p>The first level of defense should be that you always push your changes to a remote server such as github. This is usually how people work, so that if you lose your laptop you don&#39;t lose huge amounts of your work.</p><p>That said, if you have local branches or other state you don&#39;t want to lose in your local git repository, move the <code>.git</code> directory to a higher directory outside of your project directory. You can do this with a symbolic link, but git has another mechanism, too. For example if you move you repository from <code>/home/user/hacking/myproject/.git</code> to <code>/home/user/repos/myproject.git</code>, you can create an ordinary file at <code>/home/user/hacking/myproject/.git</code> that contains the text:</p><pre><code>gitdir:/home/user/repos/myproject.git\n</code></pre><p>Then git will transparently access files at the new location, which will be unavailable in strict/bare jails and copy-on-write in casual ones (unless you mask repos to avoid confusion).</p><h2 id="can-i-make-emacsclient-in-the-jail-invoke-emacs-outside-the-jail" tabindex="-1">Can I make <code>emacsclient</code> in the jail invoke emacs outside the jail? <a class="header-anchor" href="#can-i-make-emacsclient-in-the-jail-invoke-emacs-outside-the-jail" aria-label="Permalink to &quot;Can I make `emacsclient` in the jail invoke emacs outside the jail?&quot;">​</a></h2><p>You can, but it&#39;s not necessarily the best idea, because <code>emacsclient</code> can ask emacs to execute arbitrary elisp code, which means jailed code is no longer contained. This would be an extra bad idea for anything other than a casual sandbox. However, you can do it... for the sandbox named <em>jail</em>, you would put the following line in your <code>$HOME/.jai/</code><em>jail</em><code>.conf</code> file:</p><pre><code>    # works but bad idea\n    dir ${XDG_RUNTIME_DIR}/emacs\n</code></pre><p>Another issue that will arise is that <code>/tmp</code> in your jail is actually <code>/run/jai/$USER/tmp/</code><em>jail</em> outside the jail, so it won&#39;t work on files in <code>/tmp</code> and <code>/var/tmp</code>. However, you could ask a coding agent to write an emacs server proxy that listens outside the sandbox on <code>/run/jai/$USER/tmp/.run/</code><em>jail</em><code>/emacs/server</code>, filters dangerous commands, translates pathnames in <code>/tmp</code> and <code>/var/tmp</code> to <code>/run/jai/$USER/tmp/</code><em>jail</em>, and then passes the sanitized requests to your real emacs server. Coding agents are good at that kind of thing.</p><h2 id="can-i-run-xorg-applications-inside-the-jail" tabindex="-1">Can I run Xorg applications inside the jail? <a class="header-anchor" href="#can-i-run-xorg-applications-inside-the-jail" aria-label="Permalink to &quot;Can I run Xorg applications inside the jail?&quot;">​</a></h2><p>This works by default in casual sandboxes, but it&#39;s not very secure. Among other things, sandboxed code can read your clipboard (so don&#39;t be cutting and pasting passwords!), and it can use sendevents to type in other windows. If you want to do this in a bare or strict sandbox (again, not a great idea), you need to copy your Xorg authentication cookies into your sandbox. For example, to do this for the sandbox configured by <code>$HOME/.jai/claude.conf</code>, you would run:</p><pre><code>    xauth extract - $DISPLAY | jai -C claude xauth merge -\n</code></pre><p>If you <em>don&#39;t</em> want sandboxed code accessing your Xorg server even in casual mode, add the following line to <code>$HOME/.jai/.defaults</code> and run <code>jai -u</code>:</p><pre><code>    mask .Xauthority\n</code></pre><p>Or you can simply run <code>jai rm .Xauthority</code> to delete the file in your overlay.</p><h2 id="why-does-jai-use-bash" tabindex="-1">Why does jai use bash? <a class="header-anchor" href="#why-does-jai-use-bash" aria-label="Permalink to &quot;Why does jai use bash?&quot;">​</a></h2><p>jai launches jailed programs through bash to support the <code>command</code> directive in configuration files. The <code>command</code> directive lets you set environment variables, activate virtual environments, or modify the command line before execution. For example:</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>command source $HOME/venv/bin/activate; &quot;$0&quot; &quot;$@&quot;</span></span></code></pre></div><p>This would not be possible without a shell intermediary. The default command is simply <code>&quot;$0&quot; &quot;$@&quot;</code>, which runs the program directly with its arguments.</p><h2 id="can-i-use-sudo-instead-of-setuid-installation" tabindex="-1">Can I use sudo instead of setuid installation? <a class="header-anchor" href="#can-i-use-sudo-instead-of-setuid-installation" aria-label="Permalink to &quot;Can I use sudo instead of setuid installation?&quot;">​</a></h2><p>Yes. jai reads the <code>SUDO_USER</code> environment variable to determine whose home directory to sandbox:</p><div class="language-bash vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> jai</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> bash</span></span>\n<span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">sudo</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> jai</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> -j</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> claude</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> claude</span></span></code></pre></div><p>This works transparently. If both <code>SUDO_USER</code> and <code>USER</code> are set, <code>SUDO_USER</code> takes precedence. However, <code>sudo</code> usage has a few more annoyances. You will need to be root to kill <code>jai</code> from the outside. Also, by default <code>sudo</code> allocates a new pseudo-terminal device, which will be owned by root and may confuse some software.</p><h2 id="how-do-named-jails-help" tabindex="-1">How do named jails help? <a class="header-anchor" href="#how-do-named-jails-help" aria-label="Permalink to &quot;How do named jails help?&quot;">​</a></h2><p>Named jails (via <code>-j name</code>) give each tool its own home directory. This is useful when:</p><ul><li>Multiple AI tools should not see each other&#39;s API keys or configuration</li><li>You want a clean, persistent home directory for a specific tool</li><li>You want strict mode isolation (named jails default to strict)</li></ul><p>Each named jail has independent state — its own home directory, changes directory, and <code>/tmp</code>. You can have as many named jails as you want.</p><h2 id="why-not-just-docker" tabindex="-1">Why not just Docker? <a class="header-anchor" href="#why-not-just-docker" aria-label="Permalink to &quot;Why not just Docker?&quot;">​</a></h2><p>Docker is excellent for reproducible, image-based environments. But for ad-hoc sandboxing of a CLI tool on your host machine, it requires:</p><ul><li>Building or pulling an image with the tool installed</li><li>Configuring bind mounts for your working directory</li><li>Managing the impedance mismatch between container and host (UIDs, paths, shell config)</li><li>No native overlay-on-home workflow</li></ul><p>jai trades Docker&#39;s reproducibility and stronger isolation for zero-setup, host-native sandboxing. One command, no images. See the <a href="/comparison.html">Comparison</a> page for details.</p><h2 id="why-not-just-bubblewrap" tabindex="-1">Why not just bubblewrap? <a class="header-anchor" href="#why-not-just-bubblewrap" aria-label="Permalink to &quot;Why not just bubblewrap?&quot;">​</a></h2><p>bubblewrap is a powerful, flexible namespace sandbox. It can do almost everything jai does — but it requires you to explicitly assemble the filesystem view from scratch with 10+ flags. In practice, this turns into a wrapper script per tool, which is exactly the friction jai is designed to remove.</p><p>bubblewrap also cannot easily set up overlayfs (it runs unprivileged and would need fuse-overlayfs), so you lose the copy-on-write home directory that makes casual mode convenient. See the <a href="/comparison.html">Comparison</a> page for a side-by-side.</p><h2 id="why-not-just-chroot" tabindex="-1">Why not just chroot? <a class="header-anchor" href="#why-not-just-chroot" aria-label="Permalink to &quot;Why not just chroot?&quot;">​</a></h2><p>Linux documents <code>chroot</code> as <strong>not intended to be used as a security mechanism</strong>. It provides no mount namespace isolation, no PID namespace isolation, no credential separation, and is trivially escapable by root. It is not a meaningful sandbox. Any of the other tools on this page would be an improvement.</p>',69))])}const v=s(h,[["render",u]]);export{w as __pageData,v as default};
